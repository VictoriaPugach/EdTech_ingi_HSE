# Чеклист: Статус реализации образовательной платформы

> Составлен на основе: ТЗ (R1), Проектирование и тестирование (R2+R3), Figma-дизайна, текущего кода  
> Дата: 2026-06-18  

---

## 1. Уже можно тестировать

Функциональность реализована, API и UI присутствуют.

### Аутентификация (ФТ-12)
- [ ] TC-09: POST /auth/register → HTTP 201 + UUID
- [ ] TC-09: POST /auth/login → HTTP 200 + access_token (JWT)
- [ ] TC-10: GET /api/sessions без токена → HTTP 401
- [ ] TC-10: Запрос с истёкшим JWT → HTTP 401 "Token expired"
- [ ] TC-10: JWT чужого пользователя к чужой сессии → HTTP 403

### Список курсов / CoursesPage (ФТ-13 частично)
- [ ] GET /api/courses возвращает опубликованные курсы
- [ ] Карточки курсов отображаются корректно (Course_card из Figma)
- [ ] Кнопка «+ Создать курс» доступна преподавателю/админу, скрыта у студента

### Страница курса / CoursePage (ФТ-2 частично)
- [ ] GET /api/courses/:idOrSlug возвращает модули и уроки
- [ ] Программа курса разворачивается по модулям
- [ ] Блок материалов отображается (Materials_block из Figma)

### Страница урока / LessonPage
- [ ] Блоки контента (TEXT / CODE / VIDEO / IMAGE) рендерятся
- [ ] Навигация между уроками (siblings: prev/next)
- [ ] Таймер / дедлайн урока (LessonCountdown)

### Создание курса / CreateCoursePage (ФТ-2)
- [ ] POST /api/courses создаёт курс со статусом DRAFT
- [ ] Форма валидирует обязательные поля (title, language, level)

### Сессия совместного редактирования / SessionPage (ФТ-1, ФТ-2, ФТ-3)
- [ ] POST /sessions создаёт сессию → возвращает inviteCode
- [ ] POST /sessions/:id/join выдаёт sessionJWT + role
- [ ] CodeMirror 6 открывается с подсветкой синтаксиса Python/JS
- [ ] Yjs подключается к ws://localhost:4001/ws
- [ ] Ввод символа одним клиентом отображается у другого

### Hint Service (ФТ-6, ФТ-7, ФТ-8)
- [ ] TC-05: POST /api/hints с `print("Hello"` → error_type: syntax/unmatched_paren
- [ ] TC-06: POST /api/hints с `print(x)` без объявления → semantic/undefined_variable
- [ ] TC-07: Ответ не содержит слов «AST», «токен», «парсинг», «компилятор»
- [ ] TC-08: p95 < 2000 мс при 20 параллельных запросах (k6)

### CRDT (ФТ-3, НФТ-3)
- [ ] TC-01: Два Y.Doc конвергируют при одновременной вставке символов
- [ ] TC-02: Двойное удаление одного символа — идемпотентно (итог "helo")
- [ ] TC-03: Клиент буферизует операции при разрыве WS и синхронизируется после переподключения

### Инфраструктура / Health
- [ ] GET /health → {"status": "ok"}
- [ ] Docker Compose: все 6 сервисов (web, api-gateway, realtime-sync, hint-service, postgres, redis) поднимаются без ошибок

---

## 2. Реализовано в бэкенде, требует тестирования UI/E2E

БД и API есть, но UI не завершён или не проверялся.

### Управление участниками курса (задача пользователя)
- [ ] Enrollments: POST /api/courses/:id/enroll записывает студента
- [ ] Lesson progress обновляется при прохождении урока
- [ ] UI: список участников курса виден преподавателю

### Снапшоты и откат (ФТ-5)
- [ ] TC-13: GET /api/sessions/:id/snapshots → список снапшотов
- [ ] TC-13: POST /api/sessions/:id/rollback → документ откатывается
- [ ] Снапшот создаётся автоматически по достижении порога операций

### Chat в сессии — ✅ РЕАЛИЗОВАН (2026-06-18)
- [x] Живая доставка через `Y.Array('chat')` в Y.Doc сессии (ADR in-session-chat)
- [x] UI чата в SessionPage (`components/features/session/Chat`)
- [x] `GET /api/sessions/:id/chat` — история при перезаходе
- [x] `POST /api/sessions/:id/chat` — зеркало в `chat_messages` (идемпотентно по id)
- [ ] Тест: сообщение видно во втором окне мгновенно; после reload история подгружается
- [ ] Известное ограничение: VIEWER не может писать (чат едет тем же CRDT-каналом, что режется для VIEWER)

### Подключение к сессии (gap join-токена) — ✅ ЗАКРЫТ (2026-06-18)
- [x] SessionPage вызывает `POST /api/sessions/:id/join` → сессионный JWT + роль
- [x] Токен прокидывается в WS (`?token=`), realtime-sync валидирует подпись и роль
- [x] Реальная личность пользователя в awareness (имя + цвет по id)
- [x] VIEWER → редактор в режиме read-only
- [x] Fallback на анонимный режим, если сессии нет в БД (dev)

### Видеозвонок (SFU LiveKit) — ✅ РЕАЛИЗОВАН (2026-06-18)
ADR video-livekit-sfu. Требование: до 30 учеников, несколько камер, без лагов.
- [x] Сервис `livekit` в docker-compose + `infra/livekit/livekit.yaml` (Redis-кластеризация)
- [x] `POST /api/sessions/:id/livekit-token` — access-токен по роли (VIEWER без публикации)
- [x] `components/features/session/VideoCall` — simulcast (180p/360p) + dynacast + adaptiveStream
- [x] Кнопка «📹 Видеозвонок», ленивое подключение (экономия SFU/канала), LiveKit в отдельном чанке
- [x] Камера/микрофон по умолчанию выключены; экран-шеринг для публикующих
- [ ] Тест: 3+ камеры — publish-битрейт клиента не растёт с числом участников (метрики LiveKit)
- [ ] Прод: внешний IP + TURN over TCP/TLS:443, UDP-диапазон, HPA по нагрузке

### Создать занятие (кнопка преподавателя) — ✅ РЕАЛИЗОВАНА (2026-06-18)
- [x] `components/features/class/StartClassButton` — видна только teacher/admin
- [x] Создаёт групповую сессию (`mode: group`) и переходит на `/s/:id`
- [x] На HomePage; ученики подключаются по ссылке к тому же занятию (видео + код + чат)

### Achievements / Геймификация (ФТ-14)
- [ ] achievements таблица заполняется по событиям (например, исправил ошибку)
- [ ] UI: значок/анимация появляется после исправления ошибки (TC-11, шаг 9)
- [ ] Progress bar урока/курса отображается корректно (Progress_bar из Figma)

---

## 3. Нужно реализовать

Явно отсутствует или указано пользователем как незавершённое.

### 3.1 Управление участниками курса / сессии (задача пользователя — ПРИОРИТЕТ)
- [ ] UI: пригласить студента в курс (ссылка или email)
- [ ] UI: список участников сессии с индикацией онлайн (Member/Teacher, Member/Student из Figma)
- [ ] UI: список студентов курса для преподавателя
- [ ] UI: курсор и выделение участников в редакторе (Awareness Protocol, ФТ-4)
- [ ] Тест TC-12, шаг 3: аватар/курсор ученика виден преподавателю в реальном времени

### 3.2 Ссылка для подключения к сессии (задача пользователя — ПРИОРИТЕТ)
- [ ] UI: кнопка «Скопировать ссылку» на SessionPage / CoursePage
- [ ] Роут /sessions/:inviteCode → redirect в сессию или показ формы входа
- [ ] Тест TC-12, шаг 1–2: преподаватель создаёт сессию, копирует invite-ссылку, ученик присоединяется

### 3.3 Прикрепление материалов к уроку (задача пользователя)
- [ ] UI: форма загрузки/прикрепления файла или ссылки к уроку
- [ ] POST /api/lessons/:id/materials сохраняет material в БД
- [ ] UI: блок Materials_block на LessonPage отображает прикреплённые файлы
- [ ] Тест: материал появляется в GET /api/courses/:idOrSlug в поле `materials`

### 3.4 Редактирование существующих курсов и уроков (задача пользователя)
- [ ] UI: кнопка «Редактировать» на CoursePage (только для автора/admin)
- [ ] PUT /api/courses/:id — обновление метаданных курса (title, description, level…)
- [ ] UI: редактирование модулей и уроков (добавить/удалить/переупорядочить)
- [ ] UI: редактирование блоков контента урока (LessonContentBlock CRUD)
- [ ] Тест: изменение title курса отражается на CoursePage и CoursesPage

### 3.5 Страница редактирования профиля (задача пользователя)
- [ ] Роут /profile (новый) или /settings/profile
- [ ] UI: форма (name, email, avatar, ageYears) — нет экрана в Figma, нужен дизайн
- [ ] PUT /api/users/me обновляет данные профиля
- [ ] Тест: изменение имени отображается в хедере и сайдбаре

### 3.6 Страница настроек (задача пользователя)
- [ ] Роут /settings
- [ ] UI: смена пароля, язык интерфейса, уведомления — нет экрана в Figma, нужен дизайн
- [ ] PUT /api/users/me/password — смена пароля (verify old password)

### 3.7 Исправление вёрстки (задача пользователя)
- [ ] CoursesPage: карточки выровнены по сетке, нет горизонтального переполнения
- [ ] CoursePage: хедер Hello_block соответствует Figma (Property 1=Course_header)
- [ ] LessonPage: хедер соответствует Figma (Property 1=Lesson_header)
- [ ] Responsive: проверить на 1280px, 1440px, 1920px
- [ ] Сайдбар Left_column: все пункты меню кликабельны и ведут на правильные роуты

### 3.8 Статический анализ кода (задача пользователя / из тест-плана)
- [ ] SonarQube подключён к CI (GitHub Actions при каждом PR)
- [ ] 0 Critical/Blocker issues в SonarQube для core-модулей
- [ ] OWASP ZAP passive scan — 0 High-risk алертов (TC-14)
- [ ] Code coverage ≥ 80% для TI-01 (CRDT Engine), TI-04–TI-07 (Hint Service) (AC-11)

### 3.9 Hint Service — интеграция с UI (ФТ-9, ФТ-10)
- [ ] Подсветка строки с ошибкой в CodeMirror (visualCue: "highlight")
- [ ] Всплывающая плашка (Advice_card из Figma) рядом со строкой ошибки
- [ ] Кнопка «Помощь» — ручной запрос подсказки (ФТ-10), TC-11 шаг 6
- [ ] Дебаунс: анализ запускается через 1–2 сек после остановки ввода

### 3.10 Комментарий преподавателя в редакторе (ФТ-11)
- [ ] UI: преподаватель может добавить текстовую аннотацию к строке кода
- [ ] Аннотация синхронизируется через CRDT/chat и видна всем участникам
- [ ] Тест TC-12, шаг 5–6: аннотация отображается у ученика в течение 500 мс

---

## 4. Нагрузочные тесты (перед финальным релизом)

- [ ] TC-04: k6 — 10 VU × 180 сек → p95 WS latency < 100 мс (AC-02)
- [ ] TC-08: k6 — 20 VU × 30 запросов Hint API → p95 < 2000 мс (AC-03)
- [ ] AC-04: 100/100 CRDT-тест-кейсов сходятся (Vitest)
- [ ] AC-05: 0 потерянных операций при разрыве WS до 5 мин

---

## 5. Приёмочное тестирование (перед защитой)

- [ ] AC-01: 15/15 функциональных требований из ТЗ раздела 3.1 пройдены вручную
- [ ] AC-06: SUS-тест с детьми 10–14 лет → средний балл ≥ 70
- [ ] AC-07: Педагог «Инжиниум» оценил ≥ 80% подсказок как понятные (≥ 4/5)
- [ ] AC-08: Пилотное занятие → средняя оценка ≥ 4.0 / 5.0
- [ ] AC-09: 0 HTTP (не HTTPS) эндпоинтов; HSTS-заголовок присутствует
- [ ] AC-10: 0 открытых P0-багов
- [ ] AC-11: Code coverage ≥ 80% по ключевым модулям

---

## Сводка приоритетов

| Приоритет | Что делать | Секция |
|-----------|-----------|--------|
| 🔴 Сейчас | Ссылка для подключения к сессии | 3.2 |
| 🔴 Сейчас | Управление участниками / список онлайн | 3.1 |
| 🔴 Сейчас | Исправление вёрстки (курс, урок, список курсов) | 3.7 |
| 🟠 Важно | Прикрепление материалов к уроку | 3.3 |
| 🟠 Важно | Редактирование курсов и уроков | 3.4 |
| 🟠 Важно | Hint → подсветка в редакторе + кнопка «Помощь» | 3.9 |
| 🟡 Нужно | Страница профиля | 3.5 |
| 🟡 Нужно | Страница настроек | 3.6 |
| 🟡 Нужно | Комментарий преподавателя в редакторе | 3.10 |
| 🟢 Перед релизом | Статический анализ / SonarQube | 3.8 |
| 🟢 Перед релизом | Нагрузочные тесты k6 | 4 |
| 🟢 Перед защитой | Приёмочное тестирование с «Инжиниум» | 5 |

---

## Экраны Figma vs реализация

| Экран Figma | Роут | Статус |
|-------------|------|--------|
| Login | /login | ✅ Реализован |
| Home page | / | ✅ Реализован |
| My_Courses | /courses | ✅ Реализован (вёрстка нужна правка) |
| Course_Page | /courses/:id | ✅ Реализован (вёрстка нужна правка) |
| Lesson_page | /courses/:id/lessons/:id | ✅ Реализован (вёрстка нужна правка) |
| Online_class_student_Group | /s/:sessionId | 🔶 Частично (нет курсоров, нет подсказок в редакторе) |
| Online_class_student_Single | /s/:sessionId | 🔶 Частично |
| Profile / Settings | — | ❌ Не реализован, нет экрана в Figma |
| Materials upload | — | ❌ Нет экрана в Figma |
